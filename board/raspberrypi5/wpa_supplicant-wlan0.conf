ctrl_interface=/run/wpa_supplicant
update_config=1
# Optional: pin your regulatory domain (ISO 3166-1 alpha-2). Without it
# the radio adopts the country your router advertises - correct anywhere.
# country=GB

network={
    ssid="YOUR_WIFI_SSID"
    psk="YOUR_WIFI_PASSWORD"
    key_mgmt=WPA-PSK
}
