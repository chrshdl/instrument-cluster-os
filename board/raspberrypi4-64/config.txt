enable_uart=1
arm_64bit=1

disable_splash=1

kernel=u-boot.bin

dtparam=i2c_arm=on
dtparam=spi=on

# Bluetooth is not a product feature. The off-switch is the KERNEL, not a
# device-tree overlay: board/raspberrypi4-64/linux.config
# unsets CONFIG_BT, so no BT driver exists in the image and hci_uart cannot
# bind whatever the device tree says. Together with no BT firmware
# (BR2_PACKAGE_BRCMFMAC_SDIO_FIRMWARE_RPI_WIFI installs the Wi-Fi blobs only)
# and no bluez userspace, those are the three independent legs behind the RED
# radio assessment. scripts/assert-release-image.sh asserts the bluetooth
# module tree is absent from the built rootfs.

dtoverlay=vc4-kms-v3d

# -------------------
# Panel selection
# -------------------
# Raspberry Pi Touch Display 2
#dtoverlay=vc4-kms-dsi-ili9881-7inch,rotation=270,swapxy

# Waveshare 5inch DSI LCD, 800x480 (SKU WAV-18396)
dtoverlay=vc4-kms-dsi-7inch

# Waveshare 7inch DSI LCD (C), 1024x600
#dtoverlay=vc4-kms-dsi-waveshare-panel,7_0_inchC,i2c1
